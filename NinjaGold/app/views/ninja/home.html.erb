<!DOCTYPE html>
<html>
	<head>
		<title>NinjaGold!</title>
	</head>
	<body>

		<div>
			<h1>Welcome to Ninja Gold</h1>
			<p>Your Gold: <%= @gold %></p>
		</div>

		<div>
			<h3>Farm</h3>
			<p>(earns 10-20 gold)</p>
			<form action = "ninja/gold" method="POST">
				<input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
				<input type="submit" value="Find Gold!" >
				<input type="hidden" name="search" value="farm">
			</form>
		</div>

		<div>
			<h3>Cave</h3>
			<p>(earns 10-20 gold)</p>
			<form action = "ninja/gold" method="POST">
				<input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
				<input type="submit" value="Find Gold!" >
				<input type="hidden" name="search" value="cave">
			</form>
		</div>

		<div>
			<h3>House</h3>
			<p>(earns 10-20 gold)</p>
			<form action = "ninja/gold" method="POST">
				<input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
				<input type="submit" value="Find Gold!" >
				<input type="hidden" name="search" value="house">
			</form>
		</div>

		<div>
			<h3>Casino</h3>
			<p>(earns 10-20 gold)</p>
			<form action = "ninja/gold" method="POST">
				<input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
				<input type="submit" value="Find Gold!" >
				<input type="hidden" name="search" value="casino">
			</form>
		</div>

		<div>
			<h4>Activities:</h4>
			<% session[:gold_history].each do |goldHist|%>
				<p><%= goldHist %></p>
			<% end %>
		</div>

	</body>
</html>